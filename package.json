{
  "name": "anyagent",
  "version": "3.0.0",
  "description": "Small personal CLI AI agent that thinks with a local Ollama model, runs shell commands, and keeps conversations on your own machine",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "anyagent": "dist/cli.js"
  },
  "homepage": "https://anydev.ir",
  "repository": {
    "type": "git",
    "url": "https://github.com/qaderizadeh/anyagent.git"
  },
  "author": "anydev.ir",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prestart": "npm run build",
    "start": "node dist/cli.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
