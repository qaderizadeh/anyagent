{
  "name": "anyagent",
  "version": "4.0.1",
  "description": "Personal CLI agent that drives chat.deepseek.com in a real browser — a normal chat where the model writes the commands and the output gets pasted back, with no API key",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "anyagent": "dist/cli.js"
  },
  "homepage": "https://anydev.ir",
  "repository": {
    "type": "git",
    "url": "https://github.com/qaderizadeh/anyagent.git"
  },
  "author": "anydev.ir",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node test-agent.mjs && node test-live.mjs",
    "prestart": "npm run build",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "playwright": "^1.63.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
