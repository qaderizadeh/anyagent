{
  "name": "anyagent",
  "version": "5.0.0",
  "description": "Personal CLI agent that talks straight to chat.deepseek.com over HTTPS — a normal chat where the model writes one shell command at a time and the output is pasted back, with no API key and no browser",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "anyagent": "dist/cli.js"
  },
  "homepage": "https://anydev.ir",
  "repository": {
    "type": "git",
    "url": "https://github.com/qaderizadeh/anyagent.git"
  },
  "author": "anydev.ir",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node test-agent.mjs && node test-http.mjs",
    "prestart": "npm run build",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
