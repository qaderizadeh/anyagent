{
  "name": "anyagent",
  "version": "2.0.0",
  "description": "Personal CLI AI agent that drives chat.deepseek.com in a real browser — no API key, conversations stay on DeepSeek's side",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "anyagent": "dist/cli.js"
  },
  "homepage": "https://anydev.ir",
  "repository": {
    "type": "git",
    "url": "https://github.com/qaderizadeh/anyagent.git"
  },
  "author": "anydev.ir",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prestart": "npm run build",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "playwright": "^1.63.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
